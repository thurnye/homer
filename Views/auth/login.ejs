<%- include('../includes/head.ejs') %>
</head>
<body>
        <%- include('../includes/navigation.ejs') %>
        <% if (errorMessage) { %>
            <div class="err-message"><%= errorMessage %></div>
        <% } %>
    <section class="form-data">
        <form action="/login" class="form-items" method="POST">

            <label for="email">
                <div class="form-list">
                    <h3>Email:</h3>
                    <input type="email" name="email" id="" value="<%= OldUserInput.email %>">
                </div>
            </label>

            <label for="passwprd">
                    <div class="form-list">
                        <h3>Password:</h3>
                        <input type="password" name="password" id="" value="<%= OldUserInput.password %>">
                    </div>
            </label>
            <button class="btn" type="submit">Login</button>
        </form>
    </section>
    
    <%- include('../includes/head.ejs') %>